# Django Tutorial

## Creating a new "app"

In the terminal run `python manage.py startapp appName`

Add the app to the settings module, `settings.py`

```
INSTALLED_APPS = [
    '...',
    'appName'
]
```
